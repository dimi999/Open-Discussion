@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h3 class="text-center">Discussions in @ViewBag.Category.CategoryName</h3>

<div class="container">
    <h6 class="mb-3">@ViewBag.No_Discussions results in @ViewBag.Category.CategoryName</h6>

    @if (ViewBag.Message != null)
    {
        <div class="alert alert-dark" role="alert">
            @ViewBag.Message
        </div>
    }
    @{
        var categ = ViewBag.Category.CategoryId;
        var criteria = ViewBag.Criteria;
    }

    <h3 class="text-center">Discussions</h3>

    <div class="container">
        <div class="d-flex">
            <a href="/Discussion/New" class="btn btn-success">Start Discussion</a>
            <a href="/Category/Index" class="btn btn-success mx-1">View Categories</a>
            <a href="/Category/View/@categ?page=1&criteria=1" class="btn btn-success mx-1">Sort by Desceding Date (Default)</a>
            <a href="/Category/View/@categ?page=1&criteria=2" class="btn btn-success mx-1">Sort by Interaction</a>
        </div>

        @foreach (OpenDiscussionv1.Models.Discussion discussion in ViewBag.Discussions)
        {
                <div class="border my-1 p-2">
                @Html.Partial("Discussion", discussion)
                    <div class="card-body">
                        <a href="/Discussion/View/@discussion.DiscussionId" class="btn btn-info">View discussion</a>
                    </div>
                </div>
        }

        <div>
            <nav aria-label="Page navigation example">
                <ul class="pagination">
                    <li class="page-item">
                        <a class="page-link" href="/Category/View/@categ?page=1&criteria=@criteria" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                        </a>
                    </li>
                            @for (int i = 1; i <= ViewBag.lastPage; i++)
                            {
                                 <li class="page-item"> <a class="page-link" href="/Category/View/@categ?page=@i&criteria=@criteria">@(i)</a> </li>
                            }
                    <li class="page-item">
                        <a class="page-link" href="/Category/View/@categ?page=@(ViewBag.lastPage)&criteria=@criteria" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
                </ul>
            </nav>
        </div>
    </div>
</div>